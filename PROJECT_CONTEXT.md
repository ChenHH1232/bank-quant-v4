# PROJECT_CONTEXT

## 1. 文档定位

本文档用于沉淀本项目的长期背景信息、版本演进脉络、研究方法论和工程约束，作为后续分析、开发、回测、复盘与协作时的统一上下文。

本文档的目标不是记录一次性的实验细节，而是明确本项目在 V4 阶段应长期遵守的共识，包括：

- 项目要解决什么问题
- V1 至 V4 分别解决了什么问题
- 当前 V4 的明确目标是什么
- 哪些方法论约束是不可突破的硬边界
- 后续所有代码、实验、报告应围绕哪些原则展开

---

## 2. 项目背景

本项目面向中国银行板块，目标是构建一个可解释、可复现、严格避免未来函数的数据驱动选股与滚动评估体系。项目既服务于因子研究，也服务于模拟实盘条件下的策略验证。

项目最初的主线是多因子基本面选股。这个方向擅长回答“哪些银行股相对更优质、估值更合理、财务结构更健康”，但它天然存在一个不足：基本面分析更擅长做横截面比较，不足以单独判断市场或行业当前是否已经过热、是否正在进入下跌趋势、以及当前是否仍然是合适的介入时点。

这意味着，如果只依赖基本面框架，模型可能能够选出“相对好的银行股”，但仍然会在板块整体过热、趋势转弱或进入下跌阶段时给出偏机械的买入结论。

因此，V4 在延续多因子基本面研究框架的基础上，进一步引入动量理论与均值回归视角，用来补足基本面框架在市场状态识别上的不足。这个升级不是否定基本面，而是把模型从“只会选股”推进到“既能选股，也能判断买入环境”。

---

## 3. 项目目标

V4 阶段的正式目标如下：

- 构建面向中国银行股的多因子建模与筛选体系。
- 在原有基本面因子框架上，加入动量与均值回归视角，提升对行业热度、趋势切换和估值偏离的识别能力。
- 形成严格滚动、无未来信息泄露的训练与评估框架。
- 在统计层面验证因子的显著性、稳定性和可解释性。
- 将研究结果沉淀为可复现的策略信号生成流程。
- 为后续模拟实盘、风控扩展和 V5 升级提供稳定基础。

一句话概括：

V4 的目标，是把“银行股多因子基本面研究”升级为一套融合基本面、动量与均值回归视角的、无未来函数的滚动研究系统。

---

## 4. 研究对象与数据范围

### 4.1 股票池

- 研究对象为中国银行板块股票，当前口径为约 48 只银行股。
- 覆盖国有大行、股份制银行、城商行及其他具有代表性的银行上市公司。
- 股票池以数据连续性和可研究性优先，允许对退市、停牌或长期缺失样本做审慎处理，但必须保留处理记录。

### 4.2 时间范围

- 总样本期约为 15 年历史数据。
- 同时使用年度与季度数据，按统一规则映射到可交易时间轴。
- 所有财务数据必须以“当时可见”的口径进入模型，不允许使用后验修订后的未来信息。

### 4.3 数据来源结构

项目数据原则上分为三类：

- 财务数据：资产负债表、利润表、现金流量表。
- 银行专项指标：不良贷款率、资本充足率、拨备覆盖率、非息收入占比等。
- 市场数据：PB、PE、市值、股息率、价格走势、收益率、波动率及其他价格行为相关指标。

---

## 5. V1-V4 演进脉络

### 5.1 V1：问题定义与基础样本搭建

V1 的重点是明确研究范围，而不是追求复杂模型。

核心任务：

- 确定研究对象为中国银行板块，而非全市场泛行业选股。
- 明确股票池、样本期、数据字段和基础清洗口径。
- 建立最初的数据表结构与因子候选列表。
- 将“可解释性”和“防未来函数”作为一开始就写入框架的要求。

V1 的意义在于：先把研究对象定义对，把数据边界画清楚，避免后续在错误前提上持续堆功能。

### 5.2 V2：单因子与基础统计验证

V2 的重点是从“有数据”进入“有初步研究结论”。

核心任务：

- 开始做单因子检验与基础横截面分析。
- 观察各类财务因子、估值因子、银行专项因子的方向性与稳定性。
- 初步引入 IC、Rank IC、分组收益等指标，判断因子是否具备研究价值。
- 对异常值、缺失值、标准化等预处理流程形成初步规则。

V2 的意义在于：用相对简单的方法验证哪些变量值得继续研究，为后续多因子建模提供筛选基础。

### 5.3 V3：多因子建模与滚动框架雏形

V3 的重点是从静态研究走向接近真实使用场景的动态研究。

核心任务：

- 从单因子扩展到多因子组合建模。
- 引入横截面回归、Fama-MacBeth、Lasso、Ridge 等方法，比较因子筛选和系数稳定性。
- 建立训练集与测试集分离的滚动评估框架雏形。
- 开始识别并修正潜在的数据泄露、跨窗口信息污染、测试集参与预处理等问题。

V3 的意义在于：让模型不再停留在静态样本内解释，而开始接受时间推进条件下的检验。

### 5.4 V4：基本面框架升级为“基本面 + 动量 + 均值回归”

V4 是当前正式阶段，重点不是继续增加模型复杂度，而是补上过去框架中最重要的缺口：基本面分析无法独立判断板块是否过热、是否已进入趋势性下跌，以及价格是否已经偏离到需要回归修复的阶段。

核心任务：

- 保留基本面多因子框架对银行股横截面质量识别的优势。
- 引入动量理论，用于识别板块或个股是否仍处于趋势延续阶段。
- 引入均值回归视角，用于识别过热、超跌和估值修复机会。
- 将滚动窗口评估机制固定为项目主框架。
- 将所有预处理、因子筛选、模型训练严格限制在训练窗口内完成。
- 将测试窗口限定为纯预测与纯评估，不参与任何参数估计。
- 统一输出模型结果、组合表现、因子显著性和日志记录。
- 以可复现、可审计、可持续迭代为目标进行工程化整理。

V4 的阶段定位可以概括为：

不是继续“堆模型”，而是把研究框架正式升级为一套能够同时回答“买什么”和“什么时候买”的可信赖研究系统。

---

## 6. 当前方法体系

当前方法体系不再局限于基本面多因子本身，而是采用“基本面 + 动量 + 均值回归”的组合思路。三者分工如下：

- 基本面因子：负责判断“值不值得买”，解决横截面选股问题。
- 动量因子：负责判断“趋势是否仍在延续”，识别板块或个股是否处于顺势阶段。
- 均值回归因子：负责判断“是否偏离过度”，识别过热、超跌和修复机会。

三者结合后的目标不是让模型更复杂，而是让决策更完整：

- 基本面告诉我们哪些标的长期更值得关注。
- 动量帮助我们避免在趋势明显转弱时逆势配置。
- 均值回归帮助我们识别价格与估值偏离过大的状态，避免在过热阶段机械追高，也避免错过超跌后的修复机会。

### 6.1 统计与建模方法

当前优先采用可解释、可审计、便于滚动验证的方法，不以复杂黑箱模型为优先目标。

核心方法包括：

- 横截面 OLS 回归：用于刻画因子与未来收益之间的截面关系。
- Fama-MacBeth 回归：用于检验因子在时间维度上的显著性和稳健性。
- Lasso / Ridge：用于在多因子情境下做收缩与筛选，控制冗余和共线性。
- 动量相关指标：用于识别价格趋势、收益持续性和市场状态变化。
- 均值回归相关指标：用于识别估值或价格偏离长期中枢后的回归倾向。

### 6.2 方法选择原则

- 先验证因子是否真实有效，再讨论模型是否更复杂。
- 先保证时间顺序正确，再讨论拟合效果是否更高。
- 先追求稳定性和复现性，再追求局部样本上的最优结果。
- 动量和均值回归是对基本面框架的补充，不是替代。
- 所有新增因子都必须接受和基本面因子同样严格的滚动检验。

---

## 7. 滚动评估框架

### 7.1 时间结构

当前滚动框架采用以下基本口径：

- 训练窗口：5 年
- 测试窗口：2 年
- 总体采用多轮 rolling 方式推进

示意：

- 2011-2015 训练，2016-2017 测试
- 2013-2017 训练，2018-2019 测试

具体年份可根据真实样本起止时间调整，但必须保持“训练在前、测试在后、窗口隔离清晰”的原则不变。

### 7.2 每轮 rolling 必须遵守的规则

每一轮滚动都必须满足以下条件：

1. 仅使用该轮训练窗口内的数据进行建模。
2. 所有标准化、缩尾、缺失值处理规则必须只在训练窗口内拟合。
3. 测试窗口只能调用训练窗口已经确定的处理参数和模型参数。
4. 每轮必须重新进行因子筛选、参数估计与模型训练。
5. 严禁跨窗口共享任何依赖未来信息的统计结果。
6. 动量与均值回归因子的构造同样必须严格基于当时可见数据。

---

## 8. 方法论硬约束

以下约束为本项目的硬边界，任何代码、分析和回测都不得突破。

### 8.1 禁止未来函数

- 禁止使用未来时点才可获得的数据解释当前决策。
- 禁止直接或间接使用未来财报、未来标签、未来修订值。
- 禁止用未来价格路径反向构造当前可见的趋势或回归信号。

### 8.2 禁止全样本预处理

- 禁止在全样本范围内做标准化、缩尾、缺失值填充后再切分训练测试。
- 禁止让测试期数据参与任何训练期统计量计算。

### 8.3 禁止全样本因子筛选

- 禁止用全样本结果先筛因子，再回到历史上做滚动验证。
- 因子是否入选，必须由每轮训练窗口独立决定。

### 8.4 禁止隐式时间泄露

- 禁止使用未考虑披露时点的财务数据。
- 禁止把跨期拼接后的数据默认当作同一时点可见信息。
- 禁止在特征构造时引入未来窗口才能确认的信息。

### 8.5 禁止为结果好看而破坏研究纪律

- 禁止因为某次结果不理想而临时修改窗口边界、样本口径或标签定义却不留痕迹。
- 禁止只汇报表现好的窗口或表现好的组合结果。
- 禁止为了迁就单次回测结果而选择性保留某类动量或回归信号。

---

## 9. 输出要求

每一轮 rolling 至少应输出以下三类结果。

### 9.1 模型层输出

- 入选因子列表
- 因子系数
- t 值 / 显著性水平
- IC / Rank IC

### 9.2 绩效层输出

- 年化收益
- 最大回撤
- 夏普比率
- IR

### 9.3 组合层输出

- Top-N 组合收益表现，例如 Top6、Top8
- 多空组合收益表现
- 不同银行类型或不同市场状态下的横向比较结果

---

## 10. 工程结构

当前项目建议以如下目录结构组织：

```text
V4_Project/
├── data_raw/
├── data_clean/
├── factors/
├── models/
├── rolling/
├── backtest/
├── reports/
├── logs/
├── AGENTS.md
└── PROJECT_CONTEXT.md
```

目录职责应尽量单一，避免把数据清洗、因子构建、模型训练、回测统计和报告输出混杂在同一脚本中。

---

## 11. 设计原则

本项目长期遵守以下设计原则：

1. 防止数据泄露高于一切。
2. 稳定性优先于短期收益表现。
3. 可解释性优先于黑箱复杂度。
4. 可复现性优先于一次性结果。
5. 滚动一致性优先于全局最优拟合。
6. 先判断市场状态，再执行个股选择。

---

## 12. V4 阶段结论性定义

V4 不是一个单纯的“银行股多因子基本面回测项目”，而是一套面向中国银行板块的、融合基本面、动量与均值回归视角的、严格遵守时间顺序与训练测试隔离原则的、可解释且可复现的滚动研究系统。

用一句话定义：

> 面向中国银行股的、融合基本面选股、动量判断与均值回归识别的、无未来函数的严格滚动研究框架。

---

## 13. 后续扩展方向（V5 展望）

在不破坏 V4 方法论边界的前提下，后续可考虑扩展：

- 更细化的技术面因子体系，如趋势强弱、波动结构、成交结构等。
- 行业轮动或宏观状态识别模块。
- PB 均值回归及估值修复相关框架的深化。
- 风险因子分解与组合暴露控制。
- 模拟实盘执行接口与交易约束建模。

所有 V5 扩展都必须建立在 V4 已经完成“研究纪律固化”的基础上，而不是反向削弱 V4 的约束。
